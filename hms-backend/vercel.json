{
    "version": 2,
    "builds": [
      {
        "src": "server.js", 
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/((?!/static).)*",
        "dest": "server.js" 
      },
      {
        "src": "/static/(.*)",
        "dest": "static/$1"
      }
    ]
}